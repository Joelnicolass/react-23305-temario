import React from "react";
import ReactDOM from "react-dom/client";
import Estados from "./estados/Estados";

/* 
    const Componente = () => {
        return <h1>Hola Mundo</h1>
    }
*/

/* 

    export -> exportar de forma nombrada, o por defecto (por defecto una sola)
    import 

*/

ReactDOM.createRoot(document.getElementById("root")).render(<Estados />);
